/*
import { createSelector } from 'reselect';

const selectState = (state) => state.get('initiateCredit');

const makeSelectLoadingInitiateCredit = () => createSelector(
  selectState,
  (state) => state.get('loadingInitiateCredit')
);

const makeSelectInitiateCredit = () => createSelector(
  selectState,
  (state) => state.get('initiateCredit')
);

const makeSelectInitiateCreditError = () => createSelector(
  selectState,
  (state) => state.get('initiateCreditError')
);

export {
  selectState,
  makeSelectLoadingInitiateCredit,
  makeSelectInitiateCredit,
  makeSelectInitiateCreditError,
};
*/
